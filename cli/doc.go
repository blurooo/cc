// Package cli 提供了一系列针对子进程调用的封装。
// 相比官方 os.exec 库的优势：
// 1. 官方 os.exec 仅提供的是命令的调用而非脚本的执行。例如 echo "hello" 是支持的
// 但 echo $PATH 则没法按照预期执行，或者 [ -r xx ] && echo "文件存在" 这种语句。
// cli 提供的是语句的执行，且很好地解决了参数解释的问题。
// 2. cli 兼容不同操作系统，在调用层面实现了统一，支持异步、超时等能力
package cli
